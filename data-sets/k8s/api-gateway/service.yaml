apiVersion: v1
kind: Service
metadata:
  name: api-gateway-svc
  namespace: drgueria
spec:
  selector:
    app: api-gateway
  ports:
  - port: 4000
    targetPort: 4000
    name: http
  type: NodePort