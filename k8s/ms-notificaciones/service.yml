apiVersion: v1
kind: Service
metadata:
  name: ms-notificaciones-svc
  namespace: drgueria
  labels:
    monitored: "true"
spec:
  selector:
    app: ms-notificaciones
  ports:
  - port: 3005
    targetPort: 3005
    name: http