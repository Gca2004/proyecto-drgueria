apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-notificaciones
  namespace: drgueria
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-notificaciones
  template:
    metadata:
      labels:
        app: ms-notificaciones
        monitored: "true"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ms-notificaciones
          image: gustavocoque/ms-notificaciones:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3005
          envFrom:
            - configMapRef:
                name: ms-notificaciones-config
            - secretRef:
                name: ms-notificaciones-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3005
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-notificaciones-svc
  namespace: drgueria
  labels:
    app: ms-notificaciones
    monitored: "true"
spec:
  selector:
    app: ms-notificaciones
  ports:
    - name: http
      port: 3005
      targetPort: 3005
