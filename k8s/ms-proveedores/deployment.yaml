apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-proveedores
  namespace: drgueria
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-proveedores
  template:
    metadata:
      labels:
        app: ms-proveedores
        monitored: "true"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ms-proveedores
          image: drgueria/ms-proveedores:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3002
          envFrom:
            - configMapRef:
                name: ms-proveedores-config
            - secretRef:
                name: ms-proveedores-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-proveedores-svc
  namespace: drgueria
  labels:
    app: ms-proveedores
    monitored: "true"
spec:
  selector:
    app: ms-proveedores
  ports:
    - name: http
      port: 3002
      targetPort: 3002
