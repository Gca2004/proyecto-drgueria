apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-productos
  namespace: drgueria
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-productos
  template:
    metadata:
      labels:
        app: ms-productos
        monitored: "true"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ms-productos
          image: gustavocoque/ms-productos:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: ms-productos-config
            - secretRef:
                name: ms-productos-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-productos-svc
  namespace: drgueria
  labels:
    app: ms-productos
    monitored: "true"
spec:
  selector:
    app: ms-productos
  ports:
    - name: http
      port: 3001
      targetPort: 3001
