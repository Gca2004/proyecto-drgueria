apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-compras
  namespace: drgueria
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ms-compras
  template:
    metadata:
      labels:
        app: ms-compras
        monitored: "true"
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: ms-compras
          image: drgueria/ms-compras:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3003
          envFrom:
            - configMapRef:
                name: ms-compras-config
            - secretRef:
                name: ms-compras-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 15
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3003
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
---
apiVersion: v1
kind: Service
metadata:
  name: ms-compras-svc
  namespace: drgueria
  labels:
    app: ms-compras
    monitored: "true"
spec:
  selector:
    app: ms-compras
  ports:
    - name: http
      port: 3003
      targetPort: 3003
